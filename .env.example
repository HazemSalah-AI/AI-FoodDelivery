# Run python scripts/setup_env.py to generate local secrets in ignored .env.
ENVIRONMENT=development
POSTGRES_USER=delivery
POSTGRES_DB=delivery
POSTGRES_PASSWORD=
DATABASE_URL=postgresql+psycopg://delivery:GENERATED_PASSWORD@localhost:5433/delivery
JWT_SECRET=
SECURE_COOKIES=false
ALLOWED_ORIGINS=["http://localhost:5173"]
